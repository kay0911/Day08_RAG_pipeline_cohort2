# Core utilities
python-dotenv>=1.0.0
requests>=2.31.0
numpy>=1.24.0

# Task 1 — Create DOCX legal documents
python-docx>=1.1.0

# Task 2 — Web crawling
crawl4ai>=0.3.0

# Task 3 — Document conversion (PDF, DOCX → Markdown)
markitdown[docx]>=0.1.0

# Task 4 — Chunking & Indexing
langchain-text-splitters>=0.2.0
sentence-transformers>=2.7.0    # BAAI/bge-m3 embedding
chromadb>=0.5.0                  # Vector store (local, no Docker needed)

# Task 6 — BM25 lexical search
rank-bm25>=0.2.2

# Task 10 — Generation
openai>=1.30.0
# anthropic>=0.25.0             # Alternative: Claude API

# Group project — UI
streamlit>=1.35.0

# Group project — Evaluation
deepeval>=1.0.0
